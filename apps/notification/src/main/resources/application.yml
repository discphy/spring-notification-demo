server:
  port: 8081
spring:
  datasource:
    url: jdbc:h2:mem:notification;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
